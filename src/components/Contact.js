import React from 'react';
// simple contact from maybe integrated with about idk
const Contact = ({}) => (
  <div>
    <input type="text" />
    <button>Send</button>
  </div>
);

export default Contact;